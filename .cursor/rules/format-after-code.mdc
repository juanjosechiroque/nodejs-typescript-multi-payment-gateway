---
description: Run npm run format after editing source files
globs: ["src/**/*.ts"]
---

# Format After Code Changes

After creating or modifying TypeScript files, run the following once from the project root:

```bash
npm run format
```

Run only if at least one source file was modified. This keeps the codebase lint-clean and consistently formatted.
